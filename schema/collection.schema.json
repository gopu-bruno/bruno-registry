{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Registry collection entry",
  "description": "One file per collection at collection/<provider>/<owner>/<name>.json. `ns` is the git identity \"<provider>:<owner>\" (e.g. github:acme) and expands to the first two path segments. A pointer to one or more published versions of a Bruno/OpenCollection. Each version is independently sourced (git or url) — adding a version is a PR that appends to `versions`. The registry stores no usage stats; install counts come from a separate public API.",
  "type": "object",
  "required": ["ns", "name", "title", "category", "summary", "versions"],
  "additionalProperties": false,
  "properties": {
    "ns": { "type": "string", "pattern": "^[a-z0-9-]+:[a-z0-9-]+$", "description": "Namespace as \"<provider>:<owner>\" (e.g. github:acme). Expands to the collection/<provider>/<owner>/ folders." },
    "name": { "type": "string", "pattern": "^[a-z0-9-]+$", "description": "Collection name. Must match the <name> filename." },
    "title": { "type": "string", "description": "Display title shown in search and on the detail page." },
    "summary": { "type": "string", "description": "One sentence shown under the title in search." },
    "category": {
      "type": "string",
      "enum": ["payments", "ai", "auth", "devops", "comms", "data", "storage", "productivity"]
    },
    "version_tracking": { "type": "string", "enum": ["git_tags", "manual"], "description": "How versions are maintained: derived from git tags, or listed manually." },
    "verified": { "type": "boolean", "description": "EDITORIAL — set by maintainers during review. Omit when submitting." },
    "official": { "type": "boolean", "description": "EDITORIAL — set by maintainers during review. Omit when submitting." },
    "featured": { "type": "boolean", "description": "EDITORIAL — set by maintainers during review. Omit when submitting." },
    "versions": {
      "type": "array",
      "minItems": 1,
      "description": "Every published version, newest or oldest order doesn't matter (latest is derived by semver). Add one entry per version via PR; `type` may differ across versions (e.g. v1.0 from git, v1.1 from url).",
      "items": {
        "type": "object",
        "required": ["version", "type", "source"],
        "additionalProperties": false,
        "properties": {
          "version": { "type": "string", "pattern": "^v?\\d+\\.\\d+\\.\\d+(?:-[0-9A-Za-z.-]+)?$", "description": "Semver version: major.minor.patch, optional -prerelease (e.g. \"1.0.0\", \"2.1.3-beta.1\"). Compared by semver precedence to pick the latest." },
          "type": { "type": "string", "enum": ["git", "url"], "description": "How `source.url` is resolved: clone a git repo, or download a hosted artifact." },
          "source": {
            "type": "object",
            "required": ["url"],
            "additionalProperties": false,
            "properties": {
              "url": { "type": "string", "format": "uri", "description": "For type=git: the repo URL to clone. For type=url: the direct download URL of the opencollection artifact." },
              "ref": { "type": "string", "description": "git only: tag, branch or commit to check out (defaults to the repo's default branch)." },
              "subdir": { "type": "string", "description": "git only: path within the repo where the collection lives, if not the root." }
            }
          },
          "hash": { "type": "string", "description": "Optional SHA-256 of the resolved opencollection artifact, SRI-style \"sha256-<base64>\". Verified client-side after download." }
        }
      }
    }
  }
}
