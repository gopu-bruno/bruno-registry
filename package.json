{
  "name": "bruno-registry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Git catalog of OpenCollection / Bruno API collections: one publishers/<ns>/<name>.yaml file per collection. The registry server projects this catalog into its own store; there is no generated index.json.",
  "scripts": {
    "validate": "node scripts/validate.mjs"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
